 <%= form_with  model: @post, url: user_posts_path, method: :post, local: true, class: "container" do |f| %>
    <%= f.label :title, "title" %>
    <%= f.text_field :title %>
    <%= f.label :text, "post" %>
    <%= f.text_area :text %>
    <%= f.submit %>
    <% end %>
